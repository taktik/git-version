
pluginManagement {
    repositories {
        mavenLocal()
        maven { url = uri(mavenRepository) }
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'gradle-plugin-git-version'
